export async function sendDailyReady({notify=process.env.NOTIFICATION_WEBHOOK_URL}={}){if(!notify)return {status:'not_configured',message:'Notification credentials not configured.'};const response=await fetch(notify,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title:'Daily Intelligence is Ready',message:'今日全球时事、科技趋势及食品行业重点资讯已经更新。',url:'/index.html'})});return {status:response.ok?'sent':'failed',code:response.status}}
